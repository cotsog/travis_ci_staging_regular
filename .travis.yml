language: php

dist: trusty

python:
  - 7.2.4

install: true
script:
  - export CURRENT_PYTHON_VERSION=`python --version`
  - echo "$CURRENT_PYTHON_VERSION"
  - echo "Python $TRAVIS_PYTHON_VERSION"
  - if [ "$CURRENT_PYTHON_VERSION" != "Python $TRAVIS_PYTHON_VERSION" ]; then echo "false!"; false; fi
